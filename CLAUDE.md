# 프로젝트 구조 및 워크플로우

이 문서는 이 프로젝트의 조직 구조와 운영 워크플로우를 설명합니다.

## 디렉토리 구조

- `design/`: 프로젝트의 모든 기능에 대한 하이 레벨 추상화 문서들이 작성되어 있습니다. 모든 문서는 마크다운 형식이며, `main.md`를 우선적으로 참조합니다. 다른 문서의 참조가 필요한 경우, `main.md`에서 명확히 include합니다. design/ 내의 모든 문서는 반드시 프로젝트의 설계에 관한 내용을 적습니다. 프로젝트의 목적, 철학 등은 적지 않습니다. 후술할 src/ 내의 소스파일들에 대한 이정표이며, 스켈레톤 코드와 같은 역할을 합니다. 
- `src/`: 실제 프로젝트의 소스코드가 생성되는 곳입니다. src/ 내의 모든 소스코드 파일은 design/ 내의 설계 파일과 최소한 1개 이상 연결되어야 합니다. 만일 사용자가 소스코드의 구현을 지시한 경우, 사용자는 해당 지시에 대한 설계 파일을 함께 제시하여야 합니다. 만일 제시하지 못하는 경우, 개발자(에이전트, LLM 모델)은 design/ 내에 관련한 설계 파일을 먼저 작성할 것을 요청해야 합니다. 
- `interface/`: 사용자(프로젝트 설계자, 지시자)가 개발자(에이전트, LLM 모델)와 소통하기 위한 문서들이 작성되는 곳입니다. 모든 문서는 마크다운이며, 사용자의 지시사항, 추가 지시 사항, 결과 등을 정리하여 작성합니다.
- `MEMORY.md`: 개발자(LLM)이 주기적으로 컨텍스트를 유지하기 위한 파일입니다. 어떤 '작업'을 수행하기 전 해당 파일을 읽고, 그 '작업'을 수행한 이후 추가, 변경된 내용을 반영하여 기존의 `MEMORY.md`를 수정해야 합니다. `MEMORY.md`에는 현재 프로젝트의 상태(작업 중인 것, 작업 완료된 것, 해야 하는 것 등)와 사용자(설계자, 지시자)가 꼭 기억해달라고 한 중요한 명령 등을 기록합니다.

## 프로젝트 워크플로우

1.  **지시**: 사용자가 개발자(LLM)에게 어떤 작업을 지시합니다.
2.  **계획**: 작업을 수행하기 이전에 `MEMORY.md`를 읽고, `interface/` 내에 작업번호(기존과 겹치지 않는 랜덤 생성)와 함께 `{작업번호}.md` 파일을 생성합니다. 이 파일에는 어떤 작업을 어떻게 수행할 것인지, 어떤 파일이 추가되고 어떤 기능이 구현될 것인지 명확하게 설명합니다.
3.  **승인 및 실행**: 사용자가 해당 문서를 읽고 승인하는 경우, 작업을 수행한 후 그 결과를 `{작업번호}.md`에 기록합니다.
4.  **피드백 루프**: 사용자가 결과를 확인하고 추가 지시가 있는 경우, 추가 지시에 대한 작업 결과를 같은 파일에 append하고 다시 승인 절차을 밟습니다. 사용자는 추가 지시를 위하여, 해당 파일에 추가 지시를 명확히 표기하여 지시를 하고, 개발자(LLM)에게 알려줄겁니다. 이 피드백 루프 동안, 추가지시를 수행하기 전에 `MEMORY.md`를 먼저 읽고 작업을 수행합니다. 추가지시 수행 후 `MEMORY.md`를 읽을 필요는 없지만, 같은 파일에 추가지시에 대한 작업 사항을 요약해서 추가해야합니다.
5.  **완료**: 사용자가 최종적으로 해당 '작업'이 완료되었다고 승인하는 경우, 비로소 해당 '작업'은 `git commit`과 함께 완료됩니다. 이후 `MEMORY.md`에 해당 작업에 대한 요약 사항과 프로젝트 상태 업데이트(작업 중인 것에서 작업 완료된 것으로 이동 등)를 수행합니다.

모든 문서는 마크다운 형식입니다.
