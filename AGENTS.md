# 에이전트 워크플로우 및 규칙

## 핵심 워크플로우
1. **계획**: `MEMORY.md`를 읽고, 계획을 상세히 적은 `interface/{random_id}.md` 파일을 생성합니다.
2. **승인**: 실행 전 `interface/{random_id}.md` 파일에 대한 사용자의 승인을 기다립니다.
3. **피드백**: 동일한 `interface/{random_id}.md` 파일에 새로운 지시 사항이나 결과를 추가합니다.
4. **완료**: 승인이 완료되면 `git commit`을 수행하고 `MEMORY.md`를 업데이트합니다.

## 개발 규칙
- **설계 기반 개발**: `src/` 내의 모든 코드는 `design/` 내의 설계 문서와 연결되어야 합니다. 설계 파일이 없으면 요청하세요.
- **작업 추적**: 모든 작업별 문서는 `interface/`를 사용합니다.
- **컨텍스트 유지**: 작업을 시작하기 전에 항상 `MEMORY.md`를 읽고, 완료 후에는 업데이트합니다.
